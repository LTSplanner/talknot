"""Gemini へ渡す評価プロンプトの組み立て。

評価項目は config.settings.EVALUATION_CRITERIA を唯一の真実として参照する。
出力は core.models.EvaluationResult と対応する JSON に固定する。
"""
from __future__ import annotations

from config import settings
from core import meeting_context as _mc


def _meeting_context_block(context: dict | None) -> str:
    """商談の『確定情報』（営業担当・お客様・物件）を、音声より優先する前提として渡す。

    カレンダー予定と Workspace の表示名から確定できる固有名詞を明示することで、
    聞き取りに頼った当て字（例「安栗」→「アングルリ」）を防ぐ。
    """
    if not isinstance(context, dict) or not context:
        return ""

    planner = (context.get("planner_name") or "").strip()
    customer = (context.get("customer_name") or "").strip()
    prop = (context.get("property_name") or "").strip()
    case_id = (context.get("case_id") or "").strip()
    date = (context.get("meeting_date") or "").strip()

    rows = []
    if planner:
        rows.append(f"- 営業担当（＝評価対象。ライフタイムサポート側）：{planner}")
    if customer:
        rows.append(f"- お客様：{customer}")
    if prop:
        rows.append(f"- 物件：{prop}")
    meta = "／".join(
        x for x in (f"案件番号 {case_id}" if case_id else "",
                    f"実施日 {date}" if date else "") if x
    )
    if meta:
        rows.append(f"- {meta}")
    if not rows:
        return ""

    names = "／".join(f"「{n}」" for n in _mc.known_names(context))
    who = (
        f"\n★どちらが営業担当かは上の情報で確定しています。自己紹介・提案・見積り案内を"
        f"している側が {planner} です。"
        if planner else ""
    )
    return f"""
# この商談の確定情報（固有名詞はこの表記だけを使う）
{chr(10).join(rows)}
★これは録画の音声よりも優先する確定情報です。{names} は必ずこの表記で書いてください。
  音から起こしたカタカナの当て字（例「アングルリ」のような誤記）は禁止です。
★ここに無い人名（ご家族・他社の担当者など）は、音で当て字を作らず「ご主人様」「奥様」
  「お母様」「ご担当者様」などの役割呼称で書いてください。{who}
"""


def _persona_block(persona: dict | None) -> str:
    """議事録から作ったお客様ペルソナ（実データ準拠の反応傾向）を注記にする。"""
    if not isinstance(persona, dict) or not persona:
        return ""
    parts = []
    labels = {"decided": "検討済みのお客様", "undecided": "未検討のお客様"}
    for key, label in labels.items():
        p = persona.get(key) or {}
        c = "／".join((p.get("concerns") or [])[:5])
        o = "／".join((p.get("objections") or [])[:5])
        if c or o:
            parts.append(f"■{label}：よくある本音=［{c}］／断り文句=［{o}］")
    if not parts:
        return ""
    return (
        "\n# 弊社のお客様の実像（過去の商談から）\n"
        "実際の商談で頻出する反応です。営業役がこうした本音・断りを想定して"
        "先回りできていたかも評価に含めてください。\n" + "\n".join(parts) + "\n"
    )


def build_roleplay_prompt(
    scenario_lines: list[str],
    talk_script: str | None = None,
    knowledge_base: str | None = None,
    focus: str | None = None,
    persona: dict | None = None,
    meeting_context: dict | None = None,
) -> str:
    """1人ロープレ（台本のお客様 × 音声で応答）の評価プロンプト。

    添付音声は「営業役（練習者）の発話だけ」。お客様のセリフは下の台本で進行している。
    評価の構造は通常の商談評価と同じ JSON に揃える（履歴・UIをそのまま使うため）。
    meeting_context には練習者の氏名を入れる（お客様は台本の架空ペルソナなので入れない）。
    """
    base = build_evaluation_prompt(
        reference_talk=talk_script, knowledge_base=knowledge_base,
        meeting_context=meeting_context,
    )
    lines = "\n".join(f"{i+1}. お客様「{t}」" for i, t in enumerate(scenario_lines))
    focus_block = _focus_block(focus) + _persona_block(persona)
    script_note = (
        "上の『模範トーク（社内基準）』が今回の**模範トークスクリプト**です。"
        "総合スコアを付けるときは、このスクリプトの型・流れ・言い回しも参考にしてください"
        "（採点はあくまで総合スコア1本です）。"
        if talk_script
        else "模範トークスクリプトが未登録のため、住宅営業の基本の型を参考に総合スコアを付けます。"
    )
    return f"""{base}

# 【重要】これは「1人ロープレ」の音声です
- 添付音声には **営業役（練習者）の発話だけ** が入っています。お客様の声は入っていません。
- お客様は次の台本どおりに話した前提で、営業役がその間に応答しています：
{lines}
- 各録音は、上の台本のお客様セリフに対する「営業役の返答」の順番に対応します。
- {script_note}
- お客様の声が無いため、感情の読み取りは「営業役の間・トーン・言い回し」と、
  台本のお客様セリフから推測できる心理をもとに評価してください。
- feedback の timestamp は、何ターン目かを "T1" "T2" のように記してください。
{focus_block}"""


def _focus_block(focus: str | None) -> str:
    return f"\n# 【この単元で特に見るポイント（最優先）】\n{focus}\n" if focus else ""


def build_evaluation_prompt(
    reference_talk: str | None = None,
    knowledge_base: str | None = None,
    meeting_context: dict | None = None,
) -> str:
    criteria_lines = "\n".join(
        f"- {c.key} ({c.number} {c.title}): {c.description}"
        for c in settings.EVALUATION_CRITERIA
    )
    reference_block = (
        f"\n# 模範トーク（社内基準）\n{reference_talk}\n" if reference_talk else ""
    )
    knowledge_block = (
        "\n# 弊社ナレッジ（過去の商談から蓄積した社内知識。これを前提に評価する）\n"
        f"{knowledge_base}\n"
        if knowledge_base
        else ""
    )
    context_block = _meeting_context_block(meeting_context)
    return f"""{settings.SALES_AI_PERSONA}

★出力する文章はすべて日本語で書いてください（scores のコメント、hidden_needs の
signal・inferred_need・note、feedback、summary を含め、英語を混ぜない）。
{context_block}

動画（または音声）から、文字面だけでなく「声のトーン」「間」「発話比率」、さらに
言い淀み・沈黙・話題の回避・過剰な同意・即答できない様子・声の小ささなどの
非言語サインを手がかりに、お客様が言葉にしていない本音・不安・疑問（秘密領域）まで
読み取ってください。

{settings.SALES_POLICY}
{reference_block}{knowledge_block}
# 最重要：まずお客様の『隠れたニーズ（秘密領域）』を読む
- お客様の立場に立ち、商談中に「口には出していないが感じていたであろう」不安・疑問・
  期待を推定する（お客様自身もまだ自覚していないものを含む）。例：予算オーバーへの
  本当の不安、家族（親・子・実家）への遠慮、決断を先延ばしにしたい心理、本当は譲れない条件。
- 推定の根拠は必ず非言語サイン（トーン変化・間・言い淀み・話題そらし・愛想笑い等）に紐づける。
- そのうえで、営業がそのニーズに気づいて踏み込めたか／表面の言葉だけで流したかを見る。
- この「隠れたニーズへの踏み込み」の深さを、採点の中心に置く。
- 言い間違い・言い直し・単純な言い淀み・記憶の取り違え などの“機械的な現象”そのものを隠れたニーズや取りこぼし(surfaced=false)にしない（それらを根拠に潜在ニーズを断定しない）。
- ただし雑談や何気ない一言は、本音・不安が漏れる手がかりになり得るので除外しない（引き続き手がかりとして使う）。
- surfaced=false（取りこぼし）は、本当に掘るべき実質的なニーズがあったときだけ付ける。

# 採点は「総合スコア」1本で行う
採点は営業プロ視点の**総合スコア（1〜5）**の1本だけで行う。以前の「模範トーク視点 ／
営業プロ視点」の2軸別採点は廃止した。各項目は、下の『“良い商談”の定義（評価の芯）』の
5観点をバランスよく見て、総合の 1〜5 で採点する。

# 採点の基準（1〜5。辛さは“標準”）
各項目を、次のアンカーに従って総合 1〜5 で採点する。**普通にできていれば3、平均は3.5前後**
を目安に、辛すぎず甘すぎず付ける：
- 5：卓越（そのまま手本にできる）。
- 4：良い（安定して合格ライン）。
- 3：標準（普通にできた）。
- 2：あと一歩（惜しいが不足がある）。
- 1：要改善（土台から見直したい）。
重要な注意：
- 会話が滑らかなだけでは加点しない（雰囲気の良さに惑わされない）。一方で、普通にできて
  いることを低く見積もりすぎない。
- 4〜5 は具体的なタイムスタンプの根拠がある場合に付ける。判断は必ずタイムスタンプに紐づける。

# “良い商談”の定義（この5点を評価の芯にする）
次の5点をバランスで見て総合点を決める。これらが揃った姿が5点（満点）：
1) 背景理解：お客様の暮らし・家族構成・購入経緯を自然に掴めている。
2) 自然なニーズ発掘：**詰問や無理な深掘り・誘導はしない（むしろ減点）**。会話の流れの中で
   お客様自身に語らせ、こぼれた本音を自然に拾えている。
3) 商材の最大拡張：拾ったニーズを重点商材（コーティング／エコカラット／ダウンライト等）へ
   自然に広げ、**見積もりに最大限残せている**。
4) 受注への前進：次アポ・内覧会同行・見積提出など、確実に前へ進めている。
5) 信頼のグリップ力：“仲良し”ではなく「この人は信頼できる・任せたい」と思わせる誠実さ・
   プロ感・安心感。**友好≠信頼**であり、和やかなだけでは加点しない。
※②は、機械的な言い間違い・言い淀みを深掘り対象にしない方針（下記 hidden_needs）と
  矛盾させない。掘るべき実質的なニーズを、詰問せず自然に拾えたかで見る。

# 評価項目
{criteria_lines}

# 会話配分（ジョハリの窓：商談の会話時間を4領域に配分する。合計をほぼ100にする）
- open（開放領域）：お客様も私たちも知っている＝既知の確認・世間話。ここばかりは価値が低い。
- blind（盲点領域）：お客様が気づいていないが私たちは知っている＝プロからの提案・気づきの提供。
- hidden（秘密領域）：お客様は知っているが私たちは知らない＝お客様の本音・事情・不安の引き出し。
- unknown（未知領域）：双方が知らない＝対話の中で新たに見つかった気づき。
※良い商談は open（既知の確認）に偏らず、blind（提案）と hidden（本音の引き出し）に時間を割く。
  各領域のおおよその時間割合(%)を推定し、盲点・秘密にどれだけ時間を使えたかを講評する。
※comment・summary などの日本語の講評文では、必ず日本語の呼称「開放領域／盲点領域／秘密領域／未知領域」を使うこと。
  Open / Blind / Hidden / Unknown などの英語表記は絶対に使わない（例：「Blind領域」→「盲点領域」、「Hidden領域」→「秘密領域」、「Unknown領域」→「未知領域」）。

# 話者の扱い（重要）
- 評価対象は【営業担当（ライフタイムサポート側＝提案・ヒアリング・見積り案内をする人）】です。
- feedback の before / after は **営業担当の発言だけ** を書く。お客様の発言を営業の発言として書かない。
  （お客様の言葉は customer_line に、様子・感情は emotion_note や hidden_needs に書く）
- 発言の先頭に話者名（「◯◯:」等）を付けない。人物名は推測で書かない。
- 音声から誰が営業か迷う場合は、商材を提案し質問して主導している側を営業担当とみなす。
- **書き終えたら1件ずつ自己点検する**：before が customer_line と同じ／その言い換え／その一部に
  なっていたら、それは話者の取り違え。お客様の発言の**直後にある営業担当の返答**を探し直して
  before を書き直す。Before→After は「営業のトークがどう良くなるか」を見せるものであり、
  お客様の発言を改善案に置き換えるものではない。
- その場面で営業担当が実際には何も返していない（沈黙・話題が流れた）場合は、before に
  お客様の言葉を入れて埋めず、**その feedback 項目自体を出さない**。

# 発言の書き起こし精度（customer_line / before / after の文言）
- customer_line と before は **実際に話された言葉のまま** 書く。要約・言い換え・敬語の
  整形をしない（after だけが改善案の作文）。
- 聞き取れない箇所は無理に埋めず「…（聞き取り不明）」と書く。音が似ているだけの語を
  当てはめない。1文まるごと不確かなら、その feedback 項目自体を出さない。
- 弊社・商材の正式表記に必ず合わせる：ライフタイムサポート／フロアコーティング／
  エコカラット／ダウンライト／内覧会／お引渡し／オプション。音が近い別語（例「エコガラス」）
  に置き換えない。
- 人名・物件名は上の『確定情報』の表記に必ず合わせる。確定情報に無い固有名詞は、
  聞こえた音からカタカナの当て字を作らず、役割呼称（ご主人様・奥様 等）で書く。
- 金額・畳数・日付などの数字は、はっきり聞き取れたときだけ書く。曖昧なら書かない。

# 会話が成立していない商談の扱い（無理に評価しない）
言語の壁でほとんど通じていない／お客様がほとんど発話していない／画面OFFで非言語の手がかりも
乏しい 等、実質的な商談の会話が成立していない場合は、**無理に人物像やニーズを作り出さない**：
- customer_profile：attributes は付けない（空配列）。summary は「会話が十分に成立しておらず、
  確度の高い人物像は読み取れない」と正直に書く。next_approach は“この商談の実際の課題”に絞る
  （例：言語対応＝やさしい日本語／通訳／写真・視覚資料中心、まず会話が成立する土台づくり）。
- hidden_needs：根拠が薄いので出さない（空配列）。実際に語られていない前提（見積り内容など）を作らない。
- feedback / summary：会話が成立しなかった事実を踏まえ、断定や作話をしない。
- ※スコアは通常どおりのアンカーで採点する（会話が成立しないことを理由に、特別に甘く/辛くしない）。

# 出力フォーマット（JSON のみ。前後に説明文を付けない）
{{
  "one_point": {{
    "headline": "<この商談で次に直す1点。行動で20字程度。例『商品説明の前に暮らしを2問聞く』>",
    "timestamp": "MM:SS",
    "reason": "<なぜそこか＋その結果お客様がどうなったか。1〜2文>",
    "action": "<次回そのまま言える具体的なセリフ。「〜」の形で1〜2文>",
    "keep": "<続けてほしい良かった点を1つだけ・具体的に>"
  }},
  "hidden_needs": [{{
    "timestamp": "MM:SS",
    "signal": "<根拠となった非言語サイン（間・トーン・言い淀み・話題回避など）>",
    "inferred_need": "<お客様自身も言語化していない不安・疑問・本音>",
    "surfaced": <true=営業が気づき踏み込めた / false=表面で流した>,
    "note": "<踏み込めた点、または本来どう触れて深掘りすべきだったか>"
  }}],
  "scores": [{{
    "key": "<上記key>",
    "sales_score": <1-5>,
    "sales_comment": "<総合講評（上の5観点を踏まえ、良かった点＋次の一歩）>",
    "reference_score": <sales_score と同じ値をそのまま入れる（データ互換のため。UIでは表示しない）>,
    "reference_comment": ""
  }}],
  "johari": {{
    "open_pct": <開放領域の時間割合の目安 0-100>,
    "blind_pct": <盲点領域 0-100>,
    "hidden_pct": <秘密領域 0-100>,
    "unknown_pct": <未知領域 0-100>,
    "comment": "<配分の講評。盲点・秘密にもっと時間を割くための具体アドバイス>"
  }},
  "feedback": [{{
    "timestamp": "MM:SS",
    "criterion_key": "<関連する評価項目key>",
    "emotion_note": "<その瞬間のお客様の感情・様子（どう感じていたかの説明）>",
    "customer_line": "<きっかけになった【お客様】の発言を、言った言葉のまま。お客様の言葉はすべてここに書く>",
    "before": "<その直後に【営業担当（提案・ヒアリング・見積り案内をする側）】が実際に返した発言だけを、話した言葉のまま。customer_line と同じ内容を書いてはいけない。話者名の接頭辞（『◯◯:』等）は付けない>",
    "after": "<同じ営業担当がこう言えたら、という改善後の発言（営業の言葉のみ・話者名の接頭辞は付けない）>"
  }}],
  "summary": "<総合スコアを踏まえた全体のポジティブな振り返り（言葉は前向きに）>",
  "customer_profile": {{
    "attributes": ["<トーク全体から読み取れるお客様の特徴タグを1〜4個。例: せっかち/慎重/価格重視/デザイン重視/理論派/共感重視/即決/家族相談型>"],
    "summary": "<このお客様の人物像（意思決定の傾向・話し方や情報の受け取り方の好み）を2〜3文・日本語で>",
    "next_approach": "<次回このお客様にどう提案すると響くか。効く言い回しの具体例・話す順番・避けるべきこと。日本語で前向き・実践的に>"
  }},
  "knowledge": [{{
    "category": "<product=商品知識 / rule=社内ルール / technique=トーク技術 のいずれか>",
    "point": "<この商談から学べる、他の商談にも使える一般的な知識・ルール・コツ>"
  }}]
}}

hidden_needs は **0〜3件**。確かな非言語サインの根拠があるものだけを、受注への影響が
大きい順に挙げ、無理に件数を満たさない（該当が無ければ空配列 [] でよい）。
根拠の弱い憶測は書かない。
各項目について sales_score（総合スコア）と sales_comment（総合講評）を必ず出し、上のアンカーで採点してください。reference_score は sales_score と同じ値をミラーで入れ、reference_comment は空文字 "" にしてください（キーは過去データとの互換のために残しているだけです）。

# 【最重要】アウトプットは「読んだ翌日にすぐ直せる」量に絞る
分析は隅々まで綿密に行うが、**書き出すのは絞り込んだ要点だけ**にする。項目が多いと
何を直せばよいか分からなくなり、次の商談で改善されない。抽象論を捨て、具体的な
セリフで示すこと。

- one_point（次に直す1点）が最重要の出力。**この商談で最も受注に効いた欠け**を1つだけ選ぶ。
  - headline：直す行動を20字程度で（例「商品説明の前に暮らしを2問聞く」）。
  - reason：なぜそこか＋その結果お客様がどうなったかを1〜2文。MM:SS を1つ添える。
  - action：次回そのまま口に出せる**具体的なセリフ**（「〜」の形で1〜2文）。
  - keep：続けてほしい良かった点を1つだけ、具体的に。
  複数の課題があっても**必ず1つに絞る**（優先度は「受注への影響が大きい順」）。
- feedback は **3〜5件だけ**。1点アドバイスを裏づける決定的な場面に限る。
  網羅は不要で、同種の指摘を繰り返さない。会話が成立していない商談では無理に出さない。
  各件は customer_line → before → after のセットで、after は**そのまま言えるセリフ**にする。
  emotion_note は「声のトーン・間・相槌の変化」など観測した事実を短く（40字以内）。
- sales_comment は **60〜100字**。①MM:SS の場面 ②できていたこと／足りなかったこと
  ③次の一言、を短く詰める。抽象語（「傾聴できていた」「深掘りを」）だけで終えない。
- summary は **150字以内**。良かった点1つ＋次の重点1つに絞る。
- johari.comment は **60字以内**。配分の事実と、次に増やすべき領域だけ書く。
- 講評（コメント）の“言葉”は前向き・建設的に、しかし“点数”は甘くしないこと。

knowledge には、この商談から抽出できる『弊社の財産になる知識』を 0〜5 件入れてください。
- product（商品知識）/ rule（社内ルール）/ technique（トーク技術）の3カテゴリで、学びがあるものだけ。
- 必ず一般化して書く（次の商談でも使える形に）。特筆すべき学びが無ければ空配列 [] でよい。
- 個人情報は絶対に含めない（顧客名・住所・電話番号・金額などの個別具体情報は書かない）。

customer_profile は録画全体の話し方・スピード・語彙・お客様の反応から推定する。決めつけず“傾向”として書く。英語を混ぜない（属性タグも日本語）。"""
